#include <stdio.h>

int main(int argc, char const *argv[])
{
    int *ptr = NULL;

    printf("ptr 的地址是 %p\n", ptr);

    // 检查一个空指针
    // if (ptr)     // ptr 非空，则完成
    // if (!ptr)    // ptr 为空，则完成

    return 0;
}
